// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package com.azure.storage.blob.models;

import io.clientcore.core.annotations.Metadata;
import io.clientcore.core.annotations.MetadataProperties;
import io.clientcore.core.models.binarydata.BinaryData;
import io.clientcore.core.serialization.xml.XmlReader;
import io.clientcore.core.serialization.xml.XmlSerializable;
import io.clientcore.core.serialization.xml.XmlToken;
import io.clientcore.core.serialization.xml.XmlWriter;
import java.util.LinkedHashMap;
import java.util.Map;
import javax.xml.namespace.QName;
import javax.xml.stream.XMLStreamException;

/**
 * Represents the Parquet configuration.
 */
@Metadata(properties = { MetadataProperties.FLUENT })
public final class ParquetConfiguration implements XmlSerializable<ParquetConfiguration> {
    /*
     * Represents the Parquet configuration.
     */
    @Metadata(properties = { MetadataProperties.GENERATED })
    private Map<String, BinaryData> additionalProperties;

    /**
     * Creates an instance of ParquetConfiguration class.
     */
    @Metadata(properties = { MetadataProperties.GENERATED })
    public ParquetConfiguration() {
    }

    /**
     * Get the additionalProperties property: Represents the Parquet configuration.
     * 
     * @return the additionalProperties value.
     */
    @Metadata(properties = { MetadataProperties.GENERATED })
    public Map<String, BinaryData> getAdditionalProperties() {
        return this.additionalProperties;
    }

    /**
     * Set the additionalProperties property: Represents the Parquet configuration.
     * 
     * @param additionalProperties the additionalProperties value to set.
     * @return the ParquetConfiguration object itself.
     */
    @Metadata(properties = { MetadataProperties.GENERATED })
    public ParquetConfiguration setAdditionalProperties(Map<String, BinaryData> additionalProperties) {
        this.additionalProperties = additionalProperties;
        return this;
    }

    @Metadata(properties = { MetadataProperties.GENERATED })
    @Override
    public XmlWriter toXml(XmlWriter xmlWriter) throws XMLStreamException {
        return toXml(xmlWriter, null);
    }

    @Metadata(properties = { MetadataProperties.GENERATED })
    @Override
    public XmlWriter toXml(XmlWriter xmlWriter, String rootElementName) throws XMLStreamException {
        rootElementName
            = rootElementName == null || rootElementName.isEmpty() ? "ParquetConfiguration" : rootElementName;
        xmlWriter.writeStartElement(rootElementName);
        if (this.additionalProperties != null) {
            xmlWriter.writeStartElement("");
            for (Map.Entry<String, BinaryData> entry : this.additionalProperties.entrySet()) {
                xmlWriter.writeUntypedElement(entry.getKey(),
                    entry.getValue() == null ? null : entry.getValue().toObject(Object.class));
            }
            xmlWriter.writeEndElement();
        }
        return xmlWriter.writeEndElement();
    }

    /**
     * Reads an instance of ParquetConfiguration from the XmlReader.
     * 
     * @param xmlReader The XmlReader being read.
     * @return An instance of ParquetConfiguration if the XmlReader was pointing to an instance of it, or null if it was
     * pointing to XML null.
     * @throws XMLStreamException If an error occurs while reading the ParquetConfiguration.
     */
    @Metadata(properties = { MetadataProperties.GENERATED })
    public static ParquetConfiguration fromXml(XmlReader xmlReader) throws XMLStreamException {
        return fromXml(xmlReader, null);
    }

    /**
     * Reads an instance of ParquetConfiguration from the XmlReader.
     * 
     * @param xmlReader The XmlReader being read.
     * @param rootElementName Optional root element name to override the default defined by the model. Used to support
     * cases where the model can deserialize from different root element names.
     * @return An instance of ParquetConfiguration if the XmlReader was pointing to an instance of it, or null if it was
     * pointing to XML null.
     * @throws XMLStreamException If an error occurs while reading the ParquetConfiguration.
     */
    @Metadata(properties = { MetadataProperties.GENERATED })
    public static ParquetConfiguration fromXml(XmlReader xmlReader, String rootElementName) throws XMLStreamException {
        String finalRootElementName
            = rootElementName == null || rootElementName.isEmpty() ? "ParquetConfiguration" : rootElementName;
        return xmlReader.readObject(finalRootElementName, reader -> {
            ParquetConfiguration deserializedParquetConfiguration = new ParquetConfiguration();
            Map<String, BinaryData> additionalProperties = null;
            while (reader.nextElement() != XmlToken.END_ELEMENT) {
                QName elementName = reader.getElementName();

                if (additionalProperties == null) {
                    additionalProperties = new LinkedHashMap<>();
                }

                additionalProperties.put(elementName, reader.getNullableElement(BinaryData::fromObject));
            }
            deserializedParquetConfiguration.additionalProperties = additionalProperties;

            return deserializedParquetConfiguration;
        });
    }
}
